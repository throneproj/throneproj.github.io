+++
title = "Резервное копирование, обновление и перенос"
description = "Резервное копирование и восстановление Throne, перенос данных между компьютером и Android, обновление приложения для компьютера и переход со старых версий."
weight = 70
toc = true
+++

На этой странице показано, как сохранить профили и настройки в файл, восстановить их и перенести между Throne для компьютера и Throne для Android. Здесь также объясняется, как обновлять приложение для компьютера и что делать, если вы переходите со старой версии. Делайте резервную копию перед каждым обновлением.

## Резервное копирование и восстановление {#backup-restore}

Откройте `Настройки` (Settings) → `Основные настройки` (Basic Settings) → `Резервное копирование и восстановление` (Backup and Restore).

Чтобы создать резервную копию:

1. В разделе `Создать резервную копию` (Create Backup) отметьте нужные части. По умолчанию отмечены все.
2. Нажмите `Создать резервную копию` (Create Backup...) и выберите, куда сохранить файл. Предлагаемое имя — `Throne-backup.thrbackup` в вашей домашней папке.
3. Throne подтвердит создание файла и перечислит включённые в него части.

| Часть | Что содержит |
| --- | --- |
| `Профили (группы и прокси)` (Profiles (groups and proxies)) | Ваши группы, подписки и профили. |
| `Профили маршрутизации` (Routing profiles) | Ваши профили маршрутизации и их правила. |
| `Настройки` (Settings) | Все настройки, включая настройки DNS, TUN, WARP и входящих подключений. |
| `OTP профили` (OTP profiles) | Записи менеджера OTP. |
| `Пользовательские иконки` (Custom icons) | Ваши собственные иконки для трея. |

Резервная копия не содержит историю трафика, логи и файлы кэша ядра. Она также не запоминает, был ли включён `Режим TUN` (Tun Mode) или `Системный прокси` (System Proxy).

{% alert_warning() %}
Резервная копия содержит пароли ваших серверов, ссылки на подписки и другие секретные данные. Никому не передавайте этот файл и никогда не прикладывайте его к отчёту об ошибке.
{% end %}

Чтобы восстановить резервную копию:

1. Нажмите `Восстановить из копии` (Restore from Backup...) и выберите файл `.thrbackup`.
2. Throne покажет, когда была создана резервная копия. Отметьте части, которые хотите восстановить. Части, которых нет в файле, неактивны.
3. Нажмите `Восстановить` (Restore). Каждая выбранная часть заменяет ваши текущие данные. Это действие нельзя отменить.
4. Throne перезапустится.

Резервные копии из более старых версий Throne восстанавливаются как обычно. Резервную копию из более новой версии Throne может отклонить с сообщением «Неподдерживаемый формат резервной копии»; в этом случае сначала обновите Throne.

## Перенос между компьютером и Android {#desktop-android}

Throne для компьютера и Throne для Android используют один и тот же формат резервных копий, поэтому данные можно переносить в обе стороны.

С компьютера на телефон:

1. На компьютере создайте резервную копию, как описано выше.
2. Скопируйте файл `.thrbackup` на телефон.
3. На телефоне откройте `Настройки` (Settings) → `Backup & restore` (или `Инструменты` (Tools) в боковом меню), нажмите `Restore` и выберите файл. Если открыть файл из файлового менеджера, Throne тоже предложит его восстановить.
4. Отметьте части для восстановления и подтвердите. Throne перезапустится.

С телефона на компьютер:

1. На телефоне откройте `Настройки` (Settings) → `Backup & restore`, отметьте части и нажмите `Create backup`. Кнопка `Поделиться` (Share) отправляет файл в другое приложение.
2. Скопируйте файл на компьютер.
3. На компьютере восстановите его, как описано выше.

**Android:** резервные копии также можно хранить на сервере WebDAV: `Резервное копирование в WebDAV` (Back up to WebDAV), `Восстановление из WebDAV` (Restore from WebDAV) и `Настройки WebDAV` (WebDAV settings) — `Адрес сервера` (Server address), `Имя пользователя` (Username), `Пароль` (Password), `Путь резервной копии` (Backup path), `Проверить соединение` (Test connection). В Throne для компьютера нет поддержки WebDAV, но он может восстановить файл, который вы скачаете с сервера. Учётные данные WebDAV никогда не попадают в резервную копию.

При восстановлении резервной копии с компьютера на Android кое-что меняется:

| Элемент | Что происходит на Android |
| --- | --- |
| `OTP профили` и `Пользовательские иконки` | Не восстанавливаются. |
| Настройки, которые есть в обоих приложениях | Получают значения из резервной копии. Например, MTU для TUN становится 1500 (по умолчанию на Android — 9000), а уровень логирования — `info` (по умолчанию на Android — `warn`). |
| Настройки, которые есть только на Android | Сохраняют текущие значения. |
| Подключения с других устройств | Если вы восстанавливаете часть `Settings` и резервная копия разрешает подключаться другим устройствам, а на телефоне это было выключено, Throne выключает `Разрешить подключения из локальной сети` (Allow connections from the LAN) и показывает предупреждение. |
| Raw-профили маршрутизации | Сохраняются, но только для чтения. Android не может их использовать; если один из них был активен, активным становится первый пригодный профиль маршрутизации. |
| Конечные точки OpenVPN и OpenConnect в профиле маршрутизации | Сохраняются вместе с профилем, но на Android не работают. |
| Профили Tailscale и дополнительного ядра (Extra Core) | Восстанавливаются, но запустить их на Android нельзя. |

В обратном направлении правила для приложений Android и настройки прокси для отдельных приложений на компьютере ни на что не влияют.

## Обновление приложения для компьютера {#updating}

Профили и настройки хранятся в папке данных, которую обновление сохраняет (см. [папку данных](@/reference/files.ru.md#data-folder)). И всё же сначала создайте резервную копию.

1. Откройте `Утилиты` (Tools) → `Проверить наличие обновлений` (Check For Update). Throne запросит у GitHub последний релиз. Если он у вас уже установлен, вы увидите «Обновлений нет».
2. Если есть более новая версия, Throne покажет её список изменений с кнопками `Обновление` (Update), `Открыть в браузере` (Open in browser) и `Закрыть` (Close).
3. Нажмите `Обновление` (Update). Throne скачает новую версию и спросит «Обновление готово, перезапустить программу для установки?».
4. Нажмите `Да` (Yes). Throne закроется, и программа обновления установит новую версию.

Обновление одной кнопкой `Обновление` (Update) работает только для копий из ZIP-архива и для установки через установщик Windows по умолчанию, то есть для текущего пользователя, когда Throne хранит данные рядом с программой. Для остальных установок доступна только кнопка `Открыть в браузере` (Open in browser). В macOS, в пакетах с системным Qt и при установке скриптом для Linux программа обновления в комплект Throne не входит, поэтому пункт `Проверить наличие обновлений` (Check For Update) неактивен. Обновляйте каждый вид установки так:

| Способ установки | Как обновить |
| --- | --- |
| ZIP-файл | Нажмите `Обновление` (Update) или скачайте новый ZIP и замените файлы программы. Папку `config` сохраните. |
| Установщик Windows, для текущего пользователя (по умолчанию) | Нажмите `Обновление` (Update) или скачайте и запустите новый установщик. |
| Установщик Windows, для всех пользователей | Скачайте и запустите новый установщик. |
| Пакет `.deb` или `.rpm` | Скачайте и установите новый пакет. |
| Скрипт установки для Linux | Запустите скрипт ещё раз. |
| macOS | Скачайте новую версию и замените `Throne.app`. См. [Установка](@/get_started/installation.ru.md#macos). |
| WinGet, Scoop, AUR, Nix или репозиторий RPM | Используйте менеджер пакетов. См. [Менеджеры пакетов](@/get_started/installation.ru.md#package-managers). |

Если вы удаляете и заново устанавливаете Throne установщиком Windows, ответьте `Нет` (No), когда программа удаления спросит «Also delete your Throne profiles, settings and logs?» (удалить также профили, настройки и логи Throne?). Иначе ваши данные будут удалены.

Некоторые антивирусы помечают программу обновления как подозрительную, потому что она заменяет файлы программы. Если обновление не удалось, убедитесь, что Throne не находится в папке «Загрузки» ([#1109](https://github.com/throneproj/Throne/issues/1109)), и попробуйте ещё раз или скачайте новую версию со [страницы загрузок](@/downloads.ru.md).

## Бета-версии {#beta}

Бета-версии (предварительные выпуски) первыми получают новые функции и исправления, но в них может быть больше ошибок. Перед установкой бета-версии сделайте резервную копию.

- **Компьютер:** включите `Настройки` (Settings) → `Основные настройки` (Basic Settings) → `Прочее` (Miscellaneous) → `Разрешить обновление до бета-версий` (Allow updating to beta versions). После этого `Проверить наличие обновлений` (Check For Update) предлагает и предварительные выпуски, а в заголовке его окна отображается «(Pre-release)» (предварительный выпуск).
- **Android:** включите `Настройки` (Settings) → `General` → `Allow updating to beta versions`. После этого `О приложении` (About) → `Check for updates` показывает «Channel: stable and pre-releases» (канал: стабильные и предварительные выпуски). См. [встроенное средство обновления](@/android/installation.ru.md#updater).

Чтобы вернуться, выключите этот параметр. Программа обновления предлагает только версии новее установленной, поэтому вы останетесь на бета-версии, пока не выйдет более новый стабильный релиз, если только не установите стабильный релиз сами.

## Старые версии {#old-versions}

- **Throne для компьютера до версии 1.1.0.** Версия 1.1.0 перенесла все данные в один файл базы данных — `throne.db`. Старые конфигурации не конвертируются. Добавьте подписки и профили заново или попробуйте скрипты сообщества из [#1202](https://github.com/throneproj/Throne/issues/1202). `Резервное копирование и восстановление` (Backup and Restore) есть начиная с версии 1.1.3, поэтому резервные копии из версии 1.1.3 и новее восстанавливаются в более новых версиях.
- **Nekoray или NekoBox.** См. [Переход с Nekoray / NekoBox](@/help/migrating.ru.md).
- **Throne для Android до версии 2.0.0.** Версия 2.0.0 подписана новым ключом, поэтому её нельзя установить поверх более старой версии. Сначала удалите старое приложение; версия 2.0.0 запускается без данных. Старые резервные копии Android (файлы `throne_backup_*.json` и файлы `.zip` из WebDAV) восстановить нельзя — только файлы `.thrbackup`. Перед удалением запишите ссылки на свои подписки. Если вы также используете Throne для компьютера, вместо этого можно перенести всё с помощью резервной копии с компьютера. См. [Установка и обновление](@/android/installation.ru.md#upgrading).
