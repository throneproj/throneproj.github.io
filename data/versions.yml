# Release versions the Downloads page links to (without a leading "v").
# The deploy workflow overwrites this file with the latest GitHub releases before
# every build, so these values only matter for local builds.
desktop: "1.3.1"
android: "2.0.0"
